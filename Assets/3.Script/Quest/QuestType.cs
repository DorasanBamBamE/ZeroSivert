// 퀘스트 목표 종류.
//
// 원작에는 특정 지점 도달·생존 시간 같은 것도 있지만 MVP는 둘만 만든다.
// 이 둘로 원작 일일 임무의 대부분이 커버된다.
//
// 값의 순서를 바꾸면 기존 QuestData 에셋이 어긋난다. 추가는 뒤에만 할 것.
public enum QuestType
{
    Collect = 0,   // 아이템 N개를 인벤토리에 갖고 벙커로 돌아오기
    Kill = 1,      // 적 N마리 처치
}
