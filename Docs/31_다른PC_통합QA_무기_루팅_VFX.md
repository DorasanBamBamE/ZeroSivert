# 31 — 다른 PC 통합 QA · 무기 · 루팅 · VFX

작성: 2026-08-24  
기준 프로젝트: `C:/Users/m/Desktop/Backup0823/2D ZeroSivert`  
Unity: `6000.2.8f1`

## 1. 핵심 진행 실제 조작

- MainTitle에서 실제 포인터 클릭으로 `Play → LOAD GAME`을 실행하고 Hub 진입을 확인했다.
- 새 게임 초기 장비는 Makarov, 9×18mm FMJ 90발, 붕대 2개, 통조림 2개, 물 2개다.
- 역무원에게 실제 E 입력으로 접근 말풍선·대화·출발 버튼을 확인하고 Forest에 진입했다.
- Forest 탈출구에서 실제 E 입력을 5.2초 유지해 Hub로 복귀했다.
- 생존 결과 화면은 한 번만 표시됐고, 닫은 뒤 LOAD GAME을 다시 실행해도 반복되지 않았다.
- Build Settings의 Forest GUID가 옛 값으로 남은 문제를 현재 씬 GUID로 교체했다. 등록 순서는 MainTitle 0, Hub 1, Forest 2다.

## 2. Hub 전수 검사

- 바텐더·의사·네트워커·역무원 4명 모두 접근 말풍선, 실제 E 대화, 역할별 버튼, 닫기를 확인했다.
- 4명 모두 상호작용 Trigger 1개와 이동 차단 Collider 1개를 가진다.
- Hub의 활성 장애물 Collider 17개는 전부 비 Trigger이며 플레이어와 충돌한다.
- 0.5유닛 격자 경로 검사에서 플레이어 시작점부터 NPC 4명의 접근 지점까지 모두 도달 가능했다.
- 제작된 통로 예외 셀 112개는 잔여 충돌 0개, 가구 충돌 6개는 모두 유지됐다.

## 3. 무기·탄약·사운드

- 총기 12종 모두 실제 발사 처리에서 탄창이 1발 감소했다: 12/12 통과.
- 총기 12종 모두 맞는 구경 탄약을 넣고 재장전했다: 12/12 통과.
- 6개 구경별 탄약 아이템이 존재하며 발사음·재장전음·공탄음 누락은 0개다.
- 타격음은 Flesh, Dirt, Concrete, Wood, Metal 각 3개, 총 15개가 정상 로드된다.
- 인벤토리 아이콘은 총기 12종 모두 전용 참조를 가지며 Point, 16 PPU, 무압축으로 통일했다.
- `ItemView`가 원본 종횡비를 유지하도록 변경해 82% 표시 규칙은 유지하면서 가로·세로 찌그러짐을 제거했다.

## 4. 밴딧 탄약 드롭 수정

원인: 밴딧의 실제 총기 구경과 관계없이 프리팹의 고정 `5.45×39mm PS`가 떨어졌다.

수정: `WeaponDrop`이 실제 `EnemyWeapon.Data.ammoType`을 기준으로 카탈로그의 가장 저렴한 기본탄을 선택한다. 기존 `ammoItem`은 실제 총기와 같은 구경일 때만 우선한다.

검증: 밴딧 프리팹과 구조물 내 변형을 포함한 총기·드롭 조합 120건에서 구경 불일치 0건이다.

## 5. 아이템·루팅 밸런스 측정

- ItemData 70종: 고정 ID 중복 0, 아이콘 누락 0, 잘못된 그리드·가격·무게 0.
- 현재 Forest 한 회 생성: 활성 컨테이너 131개(보급 92, 의료 18, 무기 21), 적 91명, 밴딧 무기 드롭 대상 68명.
- 테이블별 10,000회 굴림 평균 기준가:

| 테이블 | 빈 결과 | 평균 수량 | 평균 기준가 |
|---|---:|---:|---:|
| 밴딧 시체 | 8.5% | 19.69 | 2,023₽ |
| 의료 상자 | 1.0% | 7.91 | 3,582₽ |
| 보급 상자 | 2.5% | 23.11 | 3,484₽ |
| 무기 상자 | 2.2% | 155.66 | 6,618₽ |

표준 루트 모델은 보급 8개, 의료 2개, 무기 2개, 밴딧 8명으로 잡았다. 시작 장비를 제외한 여유 공간을 18칸·18kg으로 제한하고 실제 상인 매입가가 높은 물건부터 적재한 20회 결과는 다음과 같다.

```yaml
상인 매입가 최소 : 7,868₽
상인 매입가 평균 : 10,590₽
상인 매입가 최대 : 13,693₽
평균 적재 무게   : 12.1kg
중앙 적재 칸 수  : 18칸
```

월드 전체 루트 총액은 크지만 실제 수익은 인벤토리 칸·무게가 제한한다. 이번 측정에서는 특정 테이블 수치의 추가 하향보다 잘못된 탄약 드롭 수정이 우선이라고 판단해 테이블 수치는 유지했다.

## 6. 탄착 VFX

- `CombatImpactVFX`를 추가했다.
- 매 타격마다 오브젝트를 생성하지 않고 최대 160개 파티클을 가진 단일 시스템을 재사용한다.
- Flesh, Metal, Wood, Dirt/Grass, Concrete에 서로 다른 색을 사용한다.
- 실제 Bullet → Hub 장애물 충돌에서 탄환 풀 복귀, 파티클 4개 생성, URP Unlit 파티클 머티리얼 연결을 확인했다.

## 7. 최종 상태

```yaml
컴파일              : 완료
Unity Console       : 오류 0 · 경고 0
Git 커밋/업로드      : 수행하지 않음
QA용 임시 에셋       : 제거
QA용 savegame/backup : LocalLow에 유지, 삭제하지 않음
```

다음 우선순위는 Forest Development Build 장시간 성능 측정, 전투·이동 체감 조정, 피격·사망·루팅·탈출의 나머지 VFX다.
